package org.maxgamer.rs.command;

/**
 * @author netherfoam The supertype for commands. You should implement this when
 *         doing different types of commands, e.g. PlayerCommands and
 *         GenericCommands (Console'able)
 */
public interface Command {
}